var gastropod = require('../');

describe('Core.Logging.Logger', function(){
	it('Indicates files coming into a stream');
	it('Indicates files leaving a stream');
	it('Does not cause a stream to stop');
	it('Allows basic templating');
});

describe('Core.Logging.ErrorHandler', function(){
	it('Catches errors instead of causing exit.');
});